<UserControl x:Class="Component.PageState.PageStateResponse"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             xmlns:mui="clr-namespace:FirstFloor.ModernUI.Windows.Controls;assembly=FirstFloor.ModernUI"
             d:DesignHeight="80" d:DesignWidth="80">
    <UserControl.Background>
        <SolidColorBrush Color="White" Opacity="0.8"  />
    </UserControl.Background>
    <UserControl.Resources>
        <SolidColorBrush Color="#FF007BE5" x:Key="CirclesColor" />
    </UserControl.Resources>
    <Viewbox Width="80" Height="80"
            HorizontalAlignment="Center"
            VerticalAlignment="Center">
        <Grid x:Name="LayoutRoot"
                Background="Transparent"
                ToolTip="请等待..."
                HorizontalAlignment="Center"
                VerticalAlignment="Center">
            <mui:ModernProgressRing IsActive="True"></mui:ModernProgressRing>
            <!--<Label Content="请等待... ..."></Label>-->
        </Grid>
    </Viewbox>
</UserControl>
